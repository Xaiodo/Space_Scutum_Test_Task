import 'package:flutter/material.dart';

class LiftError extends StatelessWidget {
  const LiftError({super.key});

  @override
  Widget build(BuildContext context) => const Center(
        child: Text('Unknown error'),
      );
}
