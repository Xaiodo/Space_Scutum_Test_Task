import 'package:flutter/widgets.dart';

class HousesError extends StatelessWidget {
  const HousesError({super.key});

  @override
  Widget build(BuildContext context) => const Expanded(
        child: Center(
          child: Text('Error'),
        ),
      );
}
